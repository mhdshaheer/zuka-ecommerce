<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sales Report</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 20px;
    }
    h1 {
      text-align: center;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 20px;
    }
    th, td {
      border: 1px solid #ddd;
      padding: 8px;
      text-align: center;
      font-size: 10px;
    }
    th {
      background-color: #f4f4f4;
      font-size: 12px;
    }
    .footer {
      margin-top: 20px;
      text-align: center;
      font-size: 12px;
      color: #666;
    }
  </style>
</head>
<body>
  <h1>Sales Report</h1>
  <p><strong>Filter:</strong> <%= filter %></p>
  <p><strong>Date Generated:</strong> <%= new Date().toLocaleString() %></p>
  <p><strong>Total Sales:</strong> <%=orders.length%></p>
  <p><strong>Total Sales Amount:</strong><%=orders.reduce((accumulator, order) => {
    return accumulator + order.finalAmount;
  }, 0)%>/-</p>
  <p><strong>Total Discount:</strong><%=orders.reduce((accumulator, order) => {
    return accumulator + order.couponDiscount;
  }, 0)%>/-</p>

  <table>
    <thead>
      <tr>
        <th>#</th>
        <th>Order ID</th>
        <th>User</th>
        <th>Discount</th>
        <th>Total Amount</th>
        <th>Date</th>
      </tr>
    </thead>
    <tbody>
      <% if (orders && orders.length > 0) { %>
        <% orders.forEach((order, index) => { %>
          <tr>
            <td><%= index + 1 %></td>
            <td><%= order._id %></td>
            <td><%= order.userId.name || 'N/A' %></td>
            <td>₹<%= order.couponDiscount.toFixed(2) %></td>
            <td>₹<%= order.finalAmount.toFixed(2) %></td>
            <td><%= new Date(order.createdAt).toLocaleDateString() %></td>
          </tr>
        <% }); %>
      <% } else { %>
        <tr>
          <td colspan="5">No orders available for the selected filter.</td>
        </tr>
      <% } %>
    </tbody>
  </table>

  <div class="footer">
    <p>Generated by Zuka E-Commerce | © <%= new Date().getFullYear() %></p>
  </div>
</body>
</html>
